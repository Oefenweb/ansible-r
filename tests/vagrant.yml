# test file for r
---
- hosts: all
  remote_user: vagrant
  sudo: true
  roles:
    - ../../
  vars:
    r_packages:
      - name: DBI
        state: present

      - name: DBI
        repos: 'http://cran-mirror.cs.uu.nl/'
        lib: /tmp

      - name: colorspace

      - name: foreign
        state: absent

      - name: Biobase
        type: bioconductor
