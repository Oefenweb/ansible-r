# test file
---
- name: converge
  hosts: all
  remote_user: vagrant
  become: true
  roles:
    - ../../
  post_tasks:
    - name: include tasks
      ansible.builtin.import_tasks: "{{ playbook_dir }}/tasks/post.yml"
